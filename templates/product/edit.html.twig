{% extends 'base.html.twig' %}

{% block title %}Modifier le produit{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Modifier le produit</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_product_index') }}"> 
                    Voir tous les produits
                </a>
            </div>
            <div class="card-body">
                {{ include('product/_form.html.twig', {'button_label': 'Modifier', 'button_color': 'btn-outline-success'}) }}
            </div>
        </div>
    </div>
{% endblock %}